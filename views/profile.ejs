<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
 <div class="w-full h-full text-white ">
     <div class="flex justify-end">
        <a class="p-2 bg-orange-600 rounded-lg mb-4 " href="/logout">logout</a>
     </div>
     <h3 class="text-3xl "><span class="text-zinc-200">Hello !</span> <%= users.name%></h3>
     <h3>You can create a new post</h3>
     <form action="/post" method="post">
        <textarea name="content" class="bg-transparent border-2 border-zinc-400 block  rounded p-1 w-72 h-40 resize-none" placeholder="Whats in your mind"></textarea>
        <input type="submit" class="bg-white text-slate-700 p-2 mt-2 rounded-lg inline-block" value="Create Post">
     </form>
     <div class="posts mt-20">
        <h3 class="mt-5 text-lg">Your Posts</h3>
        <div class="postcontainer mt-5">
            <% users.posts.forEach(function(post){  %>
                <div class=" mb-4 bg-gray-700 w-1/3 border-2 rounded-md p-4 border-zinc-700 post ">
                    <h4 class="text-yellow-600"><%= users.username%></h4>
                    <p class="text-md trcking-tight"><%= post.content%></p>
                    <div class="btns flex mt-5 gap-4">
                        <a class="text-blue-500" href="/like/<%= post.id%>">
                            <%= post.likes.indexOf(users._id)=== -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-500" href="/edit/<%= post.id%>">Edit</a>
                    </div>
                 </div>
           <% }) %>
        </div>
    </div>
     

     
 </div>
</body>
</html>